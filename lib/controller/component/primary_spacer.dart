import 'package:flutter/material.dart';

class PrimaryVerticalSpacer extends StatelessWidget {
  const PrimaryVerticalSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 32,);
  }
}
